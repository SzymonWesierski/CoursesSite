<h1>Create course</h1>

<h4></h4>

{{ form_start(form) }}

    {{ form_widget(form) }}
        <button    
            type="submit"
            class="uppercase mt-15 bg-blue-500 text-gray-100 text-lg font-extrabold py-4 px-8 rounded-3xl">
            Submit Post
        </button>

{{ form_end(form) }}